import { ProjectHistoryM } from "../model/projectHistory.model";
import { GenericRepository } from "./generic-repository";

export interface IProjectHistoryRepository extends GenericRepository<ProjectHistoryM>{
    
}