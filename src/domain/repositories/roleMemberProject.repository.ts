import { RoleMemberProjectM } from "@/domain/model/roleMemberProject.model";
import { GenericRepository } from "@/domain/repositories/generic-repository";

export interface IRoleMemberProjectRepository extends GenericRepository<RoleMemberProjectM>{
    
}